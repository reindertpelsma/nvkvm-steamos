# scratch and run logs — never commit these
*.log
*.tgz
loopdev.txt
nbd_dev.txt
data/authorized_keys
data/root_authorized_keys
nvidia/*.run
.env

# External source is always fetched by Docker and never enters its context.
nvkvm/

# Alpine netboot cache for install_steamos_vm.sh
.alpine-netboot/
